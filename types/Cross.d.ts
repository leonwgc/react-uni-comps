import React from 'react';
declare type CrossProps = {
    size?: number;
    fill?: string;
} & React.HtmlHTMLAttributes<HTMLDivElement>;
/** 用于关闭的 x */
declare const Cross: (props: CrossProps) => React.ReactElement;
export default Cross;
