
import React from 'react';
import { render, screen } from '@testing-library/react';
import { debug } from 'jest-preview';
import '@testing-library/jest-dom';
import userEvent from '@testing-library/user-event';
import TransitionElement from '../src/TransitionElement';

const title = 'TransitionElement';

describe('TransitionElement test groups', () => {
    test('render', () => {
      // render(<TransitionElement title="TransitionElement"/>);
      // const el = screen.getByTitle('TransitionElement');
      // expect(el).toBeDefined();
    });
});
